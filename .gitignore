# Entorno virtual
.venv
.venv/
.venv/*


# Archivos de audio y transcripciones generadas
*.mp3
*.wav
*.ogg
*.flac
*.opus
*.m4a
*.wma
*.aac
*_transcripcion.txt

# Claude Code
.claude/

# PyInstaller
build/
dist/
*.spec.bak

# Modelos de Whisper descargados (se bajan solos la primera vez)
modelos/

# Configuración local de quien desarrolla (la app la cambia al usarla). La de
# fábrica la genera build.ps1 desde la plantilla de gestor_modelos.py
TranscribirAudio.ini

# Vista previa del icono (se regenera con assets/hacer_icono.py)
assets/icono_muestra.png

# Entorno viejo (openai-whisper + pyannote), pendiente de borrar
.venv.old/

# Cache de Python
__pycache__/
*.pyc

# Vigilante: secretos de Nextcloud
vigilante/.env
